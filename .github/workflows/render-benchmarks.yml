name: Daily refresh

# Daily refresh of the whole site: the Benchmarks pages from the newest snapshot in
# tile-ai/TileOPs-nightly, and every page that reads the TileOPs
# checkout — the API reference from its docstrings, the mirrored design docs — since this
# job checks that repository out fresh each run. Nothing is committed; the rendered HTML
# lives on gh-pages.
#
# 22:00 UTC is 06:00 Beijing, and the TileOPs nightly finishes before that, so the data is
# waiting at the start of the working day. GitHub queues scheduled runs, so the deploy can
# land a few minutes late; workflow_dispatch is there when it should not wait.

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:

permissions:
  contents: write

# Both this job and the push deploy publish gh-pages with `--force`, so the two
# share one queue: a scheduled refresh and a merge landing together would
# otherwise race, and the loser's pages would be the ones served.
concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          # Ascend fork: upstream deletes/renames Ops on its own schedule, and
          # docs/api/*.md still lists them, so tracking upstream HEAD turns any
          # upstream refactor into a build abort here -- caeb6c78 (2026-09-02,
          # "Remove legacy contiguous GQA and MHA Ops") did exactly that, and it
          # surfaced on an unrelated push. This fork's checkout moves when we
          # move it. See PROJECT_STATE 13.84.
          repository: yyttt6/TileOPs
          path: TileOPs
          fetch-depth: 1
          sparse-checkout: |
            docs/design
            docs/perf
            src/tileops
            scripts/nightly_report.py
          sparse-checkout-cone-mode: false

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: pip install -r requirements-docs.txt

      - name: Render benchmark page from the newest snapshot
        run: bash scripts/render_bench.sh

      - name: Deploy site
        run: mkdocs gh-deploy --force
