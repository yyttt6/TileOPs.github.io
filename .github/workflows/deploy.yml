name: Deploy docs

on:
  push:
    branches: [main]
  # Ascend fork: a fork starts with Actions disabled, so the push that would
  # have deployed can land before Actions is switched on and then never fire.
  # A manual trigger means the fork can always re-deploy without an empty commit.
  workflow_dispatch:

permissions:
  contents: write

# Shared with the daily refresh: two `gh-deploy --force` runs at once would race
# to publish gh-pages.
concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: tile-ai/TileOPs
          path: TileOPs
          fetch-depth: 1
          sparse-checkout: |
            docs/design
            docs/perf
            src/tileops
            scripts/nightly_report.py
          sparse-checkout-cone-mode: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements-docs.txt
      # Regenerate the Benchmarks page from the latest nightly snapshot so a
      # push deploy never serves a stale page. Falls back to the placeholder if
      # the snapshot is unavailable.
      - run: bash scripts/render_bench.sh
      - run: mkdocs gh-deploy --force
